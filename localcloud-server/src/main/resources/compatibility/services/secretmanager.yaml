service_id: secretmanager
coverage_status: partial
operations:
  - id: secrets.lifecycle
    operation: secrets.create/list/get/delete
    status: supported
    evidence: [manual:terraform-compat-2026-05-31, terraform:api-compat-script]
    notes: Core secret CRUD works end-to-end via REST/gRPC facade.
  - id: versions.lifecycle
    operation: versions.add/access/list
    status: supported
    evidence: [manual:terraform-compat-2026-05-31, terraform:api-compat-script]
    notes: Version lifecycle with auto-creation and idempotent add/enable/disable/destroy.
  - id: advanced-secret-management
    operation: rotation, CMEK, per-secret IAM
    status: unsupported
    notes: Rotation policies, customer-managed encryption keys, and per-secret IAM are not implemented.
terraform_resources:
  status: supported
  resources: [google_secret_manager_secret, google_secret_manager_secret_version]
  evidence: [manual:terraform-compat-2026-05-31]
gcloud_paths:
  - id: gcloud.secretmanager
    status: partial
console_paths:
  - id: data-browser
    status: partial
state: { source: PostgreSQL-backed facade, seed: available, reset: available, export: available, snapshot: planned }
limitations:
  - '[prod_only] Rotation and CMEK (customer-managed encryption keys).'
  - Per-secret IAM is not complete.
ci_recommendation: Use for local and CI smoke tests that stay within listed supported operations; add a coverage assertion for this service.

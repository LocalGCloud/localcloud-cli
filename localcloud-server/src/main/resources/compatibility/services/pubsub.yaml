service_id: pubsub
coverage_status: partial
operations:
  - id: topics.lifecycle
    operation: topics.create/list/delete
    status: supported
    evidence: [manual:service-status-2026-05-26, terraform:api-compat-script]
  - id: subscriptions.lifecycle
    operation: subscriptions.create/list/delete
    status: supported
    evidence: [manual:service-status-2026-05-26, terraform:api-compat-script]
  - id: message.workflow
    operation: publish/pull/ack
    status: supported
    evidence: [manual:service-status-2026-05-26]
  - id: advanced-delivery
    operation: schemas, snapshots, seek, dead-letter policy
    status: supported
    evidence: [manual:pubsub-advanced-2026-06-22, terraform:api-compat-script]
    notes: Verified on the external Pub/Sub emulator endpoint; gateway/Terraform REST facade remains core CRUD only for advanced-delivery routes.
terraform_resources:
  status: supported
  resources: [google_pubsub_topic, google_pubsub_subscription]
  evidence: [manual:terraform-compat-2026-05-31, terraform:e2e-script]
gcloud_paths:
  - id: gcloud.pubsub
    status: partial
console_paths:
  - id: data-browser
    status: partial
state:
  source: external emulator
  seed: available
  reset: available
  export: available
  snapshot: planned
limitations:
  - External emulator supports schemas, snapshots, seek, and dead-letter policy; gateway/Terraform REST facade exposes core topic/subscription routes only.
  - gcloud and console paths remain partial/unverified for advanced Pub/Sub workflows.
ci_recommendation: Use for local and CI smoke tests across core publish/pull plus advanced-delivery smoke checks on PUBSUB_EMULATOR_HOST; keep Terraform tests within core topic/subscription CRUD unless gateway advanced routes are added.

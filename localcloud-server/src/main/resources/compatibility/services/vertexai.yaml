service_id: vertexai
coverage_status: partial
operations:
  - id: genai.stub
    operation: generateContent, streamGenerateContent, embedContent, countTokens
    status: partial
    notes: Deterministic local GenAI REST facade exists for selected flows.
  - id: model-platform
    operation: models, tuning, batch prediction
    status: unsupported
terraform_resources:
  status: planned
  resources: [google_vertex_ai_*]
gcloud_paths:
  - id: gcloud.ai-platform
    status: unverified
console_paths:
  - id: data-browser
    status: partial
state: { source: PostgreSQL-backed facade, seed: planned, reset: planned, export: planned, snapshot: planned }
limitations:
  - '[prod_only] Model training and tuning (requires TPU/GPU clusters).'
  - Prediction endpoints and model management are out of current scope.
unsupported_operations:
  - id: vertexai-batch-prediction
    operation: batchPredictionJobs.create
    surface: rest
    path_pattern: /v1/projects/{project}/locations/{location}/batchPredictionJobs
    http_status: 501
    reason: unsupported_operation
    workaround: Use deterministic generateContent/embedContent stubs for local tests.
warnings:
  - { id: vertexai-console-batch-actions, surface: console, keyword: Batch, severity: warning, message: Vertex AI batch prediction, training, tuning, and model management are not supported by LocalCloud., workaround: Use deterministic generateContent and embedContent stubs for local tests., evidence: [console:build] }
ci_recommendation: Use only for deterministic GenAI stub workflows.

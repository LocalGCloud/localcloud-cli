service_id: firestore
coverage_status: early-partial
operations:
  - id: documents.crud
    operation: documents.create/read/update/delete
    status: partial
    notes: External emulator is available; seed and browser parity needs hardening.
  - id: queries.indexes
    operation: queries/index behavior
    status: partial
    notes: Complex query/index parity is unverified.
terraform_resources:
  status: unverified
  resources: []
gcloud_paths:
  - id: gcloud.firestore
    status: unverified
console_paths:
  - id: data-browser
    status: partial
state:
  source: external emulator
  seed: partial
  reset: available
  export: planned
  snapshot: planned
limitations:
  - Seed and browser parity is not fully hardened.
  - Index/query behavior is unverified.
ci_recommendation: Use only for targeted local workflows until SDK and provisioning tests are added.

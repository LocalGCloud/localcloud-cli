[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "localcloud-cli"
dynamic = ["version"]
description = "Shared-instance LocalCloud lifecycle, project context, caller attribution, and MCP bridge"
readme = "README.md"
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = [
  "docker>=7,<8",
  "httpx>=0.28,<1",
  "mcp>=2,<3",
  "PyYAML>=6,<7",
]

[project.optional-dependencies]
test = [
  "pytest>=8,<9",
  "google-cloud-bigquery>=3,<4",
  "google-cloud-dataproc>=5,<6",
  "google-cloud-pubsub>=2,<3",
  "google-cloud-storage>=3,<4",
  "pytest-asyncio>=0.25,<1",
  "respx>=0.22,<1",
]
release = ["pyinstaller==6.14.1"]

[project.scripts]
lc = "localcloud_cli.cli:main"
localcloud = "localcloud_cli.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
[tool.setuptools.dynamic]
version = {attr = "localcloud_cli.__version__"}



[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "docker: requires a working local Docker engine",
]

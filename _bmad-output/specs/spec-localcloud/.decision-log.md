# Decision Log — spec-localcloud

**Created:** 2026-05-31
**Input sources:** AGENTS.md, docs/ARCHITECTURE.md, docs/GLOSSARY.md, docs/product/product-marketing.md, docs/product-use-cases.md, docs/license-server-prd.md, services.yaml, seed.yaml, openspec config, design-artifacts placeholder directories, and all hidden agent/spec config files (.gemini, .opencode, .cursor, .continue, .naksha).

**Mode:** Express headless — all inputs pre-collected via batch indexing. Sparse-input gaps filled as `open_questions[]`.

## Self-Validation Verdict

**Pass 1 — Coherence.** All five kernel fields are populated. Capabilities have paired intent+success (CAP-1 through CAP-8). Constraints describe non-negotiable technical bounds that actually bend design decisions (single PostgreSQL, 4GB memory, ARM64 native, single gateway). Non-goals are explicit — production replacement, full API coverage, multi-node deployment, visual workflow designer, IAM enforcement, hosted license server operation. Success signal is concrete and testable (5-minute setup, 23+ services, zero cloud dependencies). Prose is lean — decoration and backstory removed, terminology consistent with project glossary.

**Pass 2 — Preservation.** Every load-bearing claim from source documents is preserved:
- Service catalog (23+ services, external vs facade, ports, persistence) → `services.md` and `emulator-notes.md`
- Architecture decisions (7 key decisions, container layout, data flow, tech stack) → `architecture.md`
- Target personas and jobs-to-be-done → `personas.md`
- Five product use cases with concrete examples → `use-cases.md`
- License tiers, server architecture, JWT format, service gating → `license-model.md`
- Emulator-specific constraints and known behaviors → `emulator-notes.md`
- Terminology and admin API endpoints → `glossary.md`
- Build commands, test counts, code conventions → split across `architecture.md` and kernel

No load-bearing claim was dropped. Wrapper-ceremony content from source documents (narrative introductions, table-of-contents, "last updated" metadata) was intentionally omitted per Spec Law rule 7.

## Assumptions

- Developers have Docker installed (implied by "docker pull" in proof points).
- Primary GCP SDK languages: Java, Python, Go, Node.js (from use cases and tech stack).
- License tier enforcement is service-level gating (from license server PRD and AGENTS.md).
- Community tier services are Memorystore, Pub/Sub, GCS, Firestore, and basic facades (inferred from "limited subset" in product marketing; exact list is open question).
- Spark must be host-installed for Dataproc (from AGENTS.md and constraints).
- AlloyDB GetConnectionInfo returns localhost:5432 (from AGENTS.md).

## Open Questions

1. Community tier exact service list — which facade services are free vs Pro?
2. Offline/air-gapped license key rotation and expiry design for Enterprise tier
3. BigQuery SQL coverage target — 96% sufficient or roadmap to 99%?
4. Dataproc: bundle Spark vs rely on host-installed?
5. Managed cloud-hosted SaaS version vs self-hosted Docker only?
